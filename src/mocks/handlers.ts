export const handlers = [
  // rest.get("https://api.binance.com/api/v3/exchangeInfo", (req, res, ctx) => {
  //   return res(ctx.status(200), ctx.json(pairDetailsFixtures.BTCUSDT));
  // }),
  // rest.get("https://api.binance.com/api/v3/klines", (req, res, ctx) => {
  //   return res(ctx.status(200), ctx.json(bianancePriceHistoryFixture.bitcoin));
  // }),
  // rest.all("/user", (req, res, ctx) => {
  //   return res(ctx.status(403));
  // }),
];
